import React from 'react';
import { Text } from 'react-native';

const GameScreen = () => {
    return <Text style={{ textAlign: 'center', fontSize: 45 }}>Game Screen 🏇🏼</Text>;
};

export default GameScreen;
